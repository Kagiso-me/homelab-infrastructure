apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: seerr
  namespace: apps
spec:
  entryPoints:
    - websecure
  routes:
    # Authentik outpost — handles the post-auth callback and sets a session cookie
    # scoped to requests.kagiso.me. Required for forward auth to work correctly.
    - match: Host(`requests.kagiso.me`) && PathPrefix(`/outpost.goauthentik.io`)
      kind: Rule
      middlewares:
        - name: ingress-secure-headers@kubernetescrd
        - name: ingress-compress@kubernetescrd
        - name: ingress-crowdsec-bouncer@kubernetescrd
      services:
        - name: authentik-server
          namespace: auth
          port: 80
    - match: Host(`requests.kagiso.me`)
      kind: Rule
      middlewares:
        - name: ingress-secure-headers@kubernetescrd
        - name: ingress-compress@kubernetescrd
        - name: ingress-crowdsec-bouncer@kubernetescrd
        - name: ingress-auth-rate-limit@kubernetescrd
        - name: ingress-authentik-forwardauth@kubernetescrd
      services:
        - name: seerr
          port: 80
  tls: {}
