# Routes grafana.kagiso.me → Grafana systemd service on varys (10.0.10.10:3000)
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-varys
  namespace: monitoring
spec:
  type: ExternalName
  externalName: 10.0.10.10
  ports:
    - port: 3000
      targetPort: 3000
