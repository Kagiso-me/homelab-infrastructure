apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  entryPoints:
    - websecure-int
  routes:
    - match: Host(`grafana.local.kagiso.me`)
      kind: Rule
      services:
        - name: kube-prometheus-stack-grafana
          port: 80
