apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - internal-ca.yaml
  # letsencrypt-prod.yaml not active — public TLS handled by Cloudflare Tunnel,
  # private access via Tailscale. File retained as reference.
