apiVersion: v1
kind: Secret
metadata:
  name: crowdsec-secret
  namespace: crowdsec
stringData:
  # Random API key shared between LAPI and the Traefik bouncer.
  # Must match the value registered via BOUNCER_KEY_traefik in the LAPI env.
  # Generate with: openssl rand -hex 32
  bouncer-api-key: 332ac605854e0cc330a158edb61f7f3ef25a337edb197394df2cf92f7eab0656
