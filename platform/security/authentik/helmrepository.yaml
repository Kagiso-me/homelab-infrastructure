# The authentik chart is now served from the kagiso-me charts repository.
# kagiso-me HelmRepository is defined in platform/databases/helmrepository.yaml
# and shared across all platform charts — no separate HelmRepository needed here.
#
# This file is kept as a placeholder. The upstream goauthentik HelmRepository
# (https://charts.goauthentik.io) has been removed; the HelmRelease now
# references sourceRef: kagiso-me / flux-system.
