apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Alerting
  # alertmanager-config.yaml and alertmanager-secret.yaml removed —
  # Alertmanager now runs natively on varys (10.0.10.10), not in-cluster.
  # Config managed by Ansible: ansible/playbooks/services/install-monitoring.yml
  - prometheus-rules-infrastructure.yaml
  - prometheus-rules-apps.yaml
  - prometheus-rules-backups.yaml
  # Dashboards
  - grafana-dashboard-infra-overview.yaml
  - grafana-dashboard-node-deep-dive.yaml
  - grafana-dashboard-storage-disks.yaml
  - grafana-dashboard-backup-health.yaml
  - grafana-dashboard-applications.yaml
  - grafana-dashboard-databases.yaml
  - grafana-dashboard-authentik.yaml
  - grafana-dashboard-cluster-overview.yaml
